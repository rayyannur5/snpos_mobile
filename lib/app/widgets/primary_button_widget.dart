import 'package:flutter/material.dart';

class PrimaryButton extends StatelessWidget {
  const PrimaryButton({super.key, required Function onclick});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
